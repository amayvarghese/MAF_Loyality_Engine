{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/api-server run build && PORT=5173 BASE_PATH=/ NODE_ENV=production pnpm --filter @workspace/maf-loyalty run build && rm -rf public && mkdir -p public && cp -R artifacts/maf-loyalty/dist/public/. public/",
  "framework": null,
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.mjs": {
      "includeFiles": "artifacts/api-server/dist/**"
    }
  }
}
